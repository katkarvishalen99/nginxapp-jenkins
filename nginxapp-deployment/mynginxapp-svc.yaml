apiVersion: v1
kind: Service
metadata:
  labels:
    app: mynginxapp
  name: mynginxapp-svc
  namespace: nginx-ns
spec:
  ports:
  - nodePort: 30495
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: mynginxapp
  type: NodePort
